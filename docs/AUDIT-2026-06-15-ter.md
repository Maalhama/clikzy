# Audit complet Cleekzy — 2026-06-15 (3e passe, post-fix PII)

6 agents parallèles (sécu DB, self-exclusion, paiements, RGPD/CNIL, régression fix P1.1, qualité code). Chaque finding vérifié dans le code. Baseline : tsc clean, lint 0/0, 81 tests, build OK.

## Corrigé & shippé (commit `4ca340b`, déployé)

- **P1 — RÉGRESSION du fix RLS own-row** : `getMyClan` (`clans.ts:36`) lisait username/level/xp des autres membres via un embed `profiles(...)` sur le client session → `null` en own-row → roster « Joueur / niveau 1 / 0 XP ». **Fonction DEFINER `get_clan_members`** (migration `20260615220001`, appliquée + testée) + `clans.ts` l'utilise. *(Trouvé par l'agent régression — je l'avais manqué.)*
- **P0 — CNIL** : Sentry chargeait côté client avec `tracesSampleRate: 0.1` (tracing perf = traceur soumis à consentement) sans gate. Passé à **`0`** → capture d'erreurs seule (intérêt légitime, sécurité/débogage), sans transactions ni replay.
- **P2 — jeu responsable** : `openChest` (tirage hasard) n'était pas gardé → un compte en pause pouvait ouvrir des coffres. Garde `selfExcludedUntil` ajoutée.
- **P1 — RGPD** : export complété (comments, pack_purchases, cadeaux reçus, self-exclusion, mini-jeux, badges, appartenance clan — en plus de profil/gagnants/achats/cadeaux envoyés).
- types Supabase régénérés (SSOT : `get_public_profile`/`get_public_profiles`/`get_clan_members` présents).

## Confirmé SAIN par les agents (non-findings vérifiés)

- **Sécu DB** : la fuite `profiles` est bien fermée ; toutes les autres tables sensibles (achats, packs, gift_codes, stripe_events, inventaires, user_fairness) sont own-row ou default-deny. Aucune DEFINER sans `search_path`, zéro `EXECUTE format` (pas d'injection). RLS activée sur les 33 tables, aucune policy d'écriture `USING(true)`.
- **Self-exclusion** : inviolable sur les 6 chemins de dépense (clic + double barrière trigger DB, mini-jeux, 3 checkouts Stripe, gift, buy-it-now). GREATEST anti-raccourcissement, RLS lecture-seule.
- **Paiements** : idempotence webhook (stripe_events + ledgers par session), x2 non farmable (atomique), prix 100% serveur, refund auto sur paiement-sans-crédit, reset cron n'écrase pas `earned_credits`.
- **RGPD** : gate consentement Umami OK, bandeau avec vrai refus, suppression de compte effective (FK CASCADE/SET NULL, pas de PII résiduelle), gate 18+ couvre Google OAuth, pages légales sans `<main>` imbriqué, jeu-responsable exact.

## Reporté (documenté, non bloquant)

- **P3** `winners.tracking_number` lisible via PostgREST direct (semi-PII) — manque un GRANT colonne comme `profiles` (risque d'énumération, à faire prudemment).
- **P3** trigger self-exclusion contournable par insert `clicks` brut `is_bot=true` (impact faible : pas de re-entrée réelle en enchère) — baser la détection bot sur `user_id IS NULL`.
- ~~**P2 paiements**~~ → **CORRIGÉ** (commit `61aaabc`) : handler `charge.refunded`/`dispute` → RPC `clawback_pack_credits` (reprend les earned_credits, idempotent, borné 0) ; VIP `past_due`/non-payant downgrade immédiat ; fallback `current_period_end` manquant = 48h + alerte (au lieu de +30j). ⚠️ Mehdi : activer ces events sur le webhook Stripe.
- **P1 edge** buy-it-now : 2 sessions Stripe sur le même item = double paiement non remboursé (`record_buy_it_now` renvoie `ok:true` sur conflit).
- **P2 qualité** : ~80 `as any` supprimables (le bug ssr ne touche que `.rpc` du server client, pas `.from()` ni le service client) ; 3 exports morts ; clients raw-js admin sans `<Database>`.
- **P2 jeu responsable** : un compte en pause peut se reconnecter (pas de middleware/blocage de session) — toutes les dépenses échouent quand même.
- **Rédactionnel/juridique** : privacy déclare « pas de tracking tiers » (à corriger : Umami + Sentry) ; médiateur conso `null` (adhésion = action Mehdi) ; qualification ANJ « pas un jeu de hasard ».

---
*3e passe. La régression clan (introduite par le fix PII) est corrigée, le P0 Sentry fermé. Le socle est solide ; les reportés sont soit P2/P3 non bloquants, soit pré-lancement (paiements edge), soit juridiques (Mehdi).*
